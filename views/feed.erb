<ul class="feed">
  <% @posts.each do |post| %>
  <li class="feed_item med_width">
  <p class="html_text"><%= post[:text] %></p>
  <p class="credit med_width">via <a href="<%= post[:url] %>"><%= post[:student_name] %></a></p>
  <% end %>
  </li>
</ul>